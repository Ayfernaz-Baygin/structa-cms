# Server-side base URL for the Structa API (used by the admin's /api/* proxy routes). Not exposed to the browser.
API_URL=http://localhost:4000

# Browser-reachable API URL, used only for uploaded media previews.
NEXT_PUBLIC_API_URL=http://localhost:4000
